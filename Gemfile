source "https://rubygems.org"

# Local preview only. GitHub Pages builds this site on its own, server-side —
# nothing here is needed for the published site to work.
#   bundle install
#   bundle exec jekyll serve   # then open http://localhost:4000
gem "jekyll", "~> 4.4"
gem "webrick"
